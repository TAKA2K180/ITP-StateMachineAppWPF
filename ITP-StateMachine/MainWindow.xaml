<Window x:Class="ITP_StateMachine.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:mah="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        xmlns:local="clr-namespace:ITP_StateMachine"
        xmlns:viewmodel ="clr-namespace:ITP_StateMachine.ViewModels"
        mc:Ignorable="d"
        Title="MainWindow" FontFamily="Cascadia Mono" Height="450" Width="800" WindowStartupLocation="CenterScreen" Background="#213038">
    <Grid>
        <!--<Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>-->

        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <mah:ProgressRing Foreground="White"
                  IsActive="{Binding LoadingVisilibility}" VerticalAlignment="Bottom"/>
        <TextBlock Text="{Binding DeviceStatus}" HorizontalAlignment="Center" VerticalAlignment="Top" FontSize="30" Foreground="White" Grid.Row="1"/>

    </Grid>
</Window>
